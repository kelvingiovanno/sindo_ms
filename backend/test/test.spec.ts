import { InventoryStatus, UserRole } from '../generated/prisma/client';

describe('test', () => {
    it('get inventory status', () => {
        console.log(UserRole);
        console.log(InventoryStatus);
    });
});
